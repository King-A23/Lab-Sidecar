# Course Project Goal

Build a compact image classifier that is accurate enough for a course demo and fast enough for interactive inference.

The weekly report should compare the baseline, improved baseline, transformer candidate, best accuracy model, and quantized deployment candidate. The final artifacts should be suitable for a written report and a short presentation without requiring Lab-Sidecar to generate a PPTX file in V1.
